// Java is fully synced. 

//class.scrollY class.scrollX

// If we are working with ID 
// class.getID

const grid = document.getElementById (grid)

const gird = document.querySelector ("#nameofID"); // accpents any elements, Id, class, etc. But we need to let know the selector the type of element. 
//gives you only the first element. 

const example = document.querySelectorAll (".nameOfClass") // All will give you all elements. 

//to get/set/remove an atrribute to html through javascript 

div.getAttribute();

// <p class="testPragraph">Hello</p>
p.append("")

//create a button 

const myNewButton = document.createElement ("button");

myNewButton.textContent = "I'm a button";
myNewButton.setAttribute ("calss, button")

p.appendChild (myNewButton);


